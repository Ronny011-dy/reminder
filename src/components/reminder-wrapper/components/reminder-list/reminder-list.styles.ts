import styled from 'styled-components';

const Root = styled.div``;

const ChildReminder = styled.div`
  margin-left: 50px;
  border-style: none none none solid;
  border-color: rgba(198, 198, 198, 0.6);
  border-width: 1px;
`;

export { Root, ChildReminder };
