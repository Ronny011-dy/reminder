import styled from 'styled-components';

const Root = styled.div`
  .add {
    transition: all 0.15s ease-in-out;
    opacity: 25%;
    &:hover {
      opacity: 100%;
    }
  }
  .plus {
    height: 25px;
  }
`;

export { Root };
