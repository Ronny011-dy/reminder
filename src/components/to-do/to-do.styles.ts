import styled from 'styled-components';

export const Root = styled.div`
  overflow-x: auto;
  margin-bottom: 20px;
  .tags {
  }

  .row {
    display: flex;
    flex-direction: row;
    align-items: center;
  }

  .padding {
    width: 56px;
  }
`;
