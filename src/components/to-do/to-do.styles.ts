import styled from 'styled-components';

const Root = styled.div`
  overflow-x: auto;
  margin-bottom: 20px;

  .row {
    display: flex;
    flex-direction: row;
    align-items: center;
  }

  .padding {
    width: 56px;
  }
`;

export { Root };
